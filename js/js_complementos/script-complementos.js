/* const scrollImagen = document.querySelectorAll(".item");

function mostrarScroll() {
  let scrollTop = document.documentElement.scrollTop;
  for (let i = 0; i < scrollImagen.length; i++) {
    let altura = scrollImagen[i].offsetTop;
    if (altura - 500 < scrollTop) {
      scrollImagen[i].style.opacity = 1;
      scrollImagen[i].classList.add("mostrarArriba");
    }
  }
}

window.addEventListener("scroll", mostrarScroll); */
